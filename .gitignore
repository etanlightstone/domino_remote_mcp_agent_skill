# Domino MCP tokens (stored in ~/.domino-mcp/, not here, but just in case)
tokens.json

# IDE local settings (MCP configs are committed; user settings are not)
.claude/settings.local.json
.cursor/rules/
.kiro/agents/

# Python
__pycache__/
*.pyc

# OS
.DS_Store
